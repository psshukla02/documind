# Copy to .env.production (or set as a repository variable on GitHub)
# before running `npm run build`.
#
# This must point to the Render backend and INCLUDE the /api suffix.

VITE_API_BASE=https://documind-backend.onrender.com/api
